{{define "footerv"}}
<script>
$(function(){
	$('audio,video').mediaelementplayer({
		loop: false,
		shuffle: false,
		playlist: false,
		audioHeight: -200,
		playlistposition: 'top',
		startVolume: 0.2,
		enableKeyboard: true,
		features: ['playlistfeature', 'prevtrack', 'playpause', 'nexttrack', 'loop', 'shuffle', 'playlist', 'current', 'progress', 'duration', 'tracks', 'volume', 'fullscreen'],
	});
});
</script>

</body>
</html>
{{end}}
